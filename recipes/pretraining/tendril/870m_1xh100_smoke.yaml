name: tendril_870m_1xh100_pretraining_smoke
description: Tendril 870m 1xH100 pretraining.

config:
  run:
    name: tendril_870m_1xh100_pretraining_smoke

  runtime:
    device_type: cuda
    training_precision: bf16
    use_torch_compile: true
    use_fsdp: false
    number_of_cpu_processes: 16

  training:
    stage: pretraining
    seed: 42
    total_batch_size: 131072
    max_steps: 20
    early_stopping_patience: 100
    early_stopping_patience_skip_steps: 0

  model:
    architecture: tendril
    dim: 1280
    n_layers: 36
    n_heads: 10
    n_kv_heads: 5
    multiple_of: 1024
    ffn_dim_multiplier: 1.0
    norm_eps: 1.0e-5
    rope_theta: 500000.0
    max_batch_size: 8
    max_seq_len: 2048

  tokenizer:
    checkpoint_path: HuggingFaceTB/SmolLM2-360M
    huggingface_tokenizer: true
    ignore_index: -100

  paths:
    datasets:
      pretraining_path: ./datasets/pretraining
    evals:
      hellaswag_path: ./datasets/hellaswag
      winogrande_path: ./datasets/winogrande
      arc_challenge_path: ./datasets/arc_challenge
    runs:
      output_dir_path: ./runs
    test_prompts_path: ./test_prompts.json

  optimizers:
    adamw:
      min_lr: 3.0e-5
      max_lr: 3.0e-4
      weight_decay: 0.1
      betas: [0.9, 0.95]
      use_fused: null
      warmup_steps: 2
    muon:
      enabled: false

  validation:
    every_x_steps: 5
    validation_steps: 20
    run_on_first_step: true
    run_on_last_step: true

  generation:
    every_x_steps: 5
    max_test_gen_len: 64
    run_on_first_step: true
    run_on_last_step: true

  evals:
    hellaswag:
      every_x_steps: 5
      number_of_examples: 50
      run_on_first_step: true
      run_on_last_step: true
    winogrande:
      every_x_steps: 5
      number_of_examples: 50
      run_on_first_step: true
      run_on_last_step: true
    arc_challenge:
      every_x_steps: 5
      number_of_examples: 50
      run_on_first_step: true
      run_on_last_step: true

  checkpointing:
    save_checkpoints: true
    save_best_only: false
    every_x_steps: -1
    max_number_checkpoints: 3
    run_on_first_step: true
    run_on_last_step: true

  wandb:
    enabled: true

data:
  seed: 42
  shard_size: 100000000

  datasets:
    HuggingFaceTB/smollm-corpus:
      fineweb-edu-dedup:
        weight: 1.0
        transforms:
          max_datapoints: 50000

  evals:
    hellaswag:
      enabled: true
    winogrande:
      enabled: true
    arc_challenge:
      enabled: true
